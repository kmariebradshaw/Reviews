<section class="accent-padding background-green page-header">
	<h1 class="tac">View / Edit Products</h1>
</section>
<section class="main">
<% @products.each do | product | %>
	<p><%= product.title %>: <%= link_to 'Edit', edit_product_path(product) %> | <%= link_to 'Delete', product_path(product),
              method: :delete,
              data: { confirm: 'This will delete all associated reviews, are you sure?' } %></td> 
              <% if product.status == "approved" %> |
 				Archive </p>
    		 <% else %>
    		 	| Display </p>
    		 <% end %>
<% end %> 
</section>